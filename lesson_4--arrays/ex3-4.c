#include <stdio.h>

int tradutor(int array1[], int array2[], int size) {
    int array3[];
    for (int i = 0; i < size; i++) {
        if (array1[i] == array2[i]) {
            array3[] += array1[i];
        }
    }
    return array3[];
}
